;;; accumulate.el --- Accumulate (exercism)  -*- lexical-binding: t; -*-

;;; Commentary:

;;; Code:


(defun accumulate (lst op)
  (mapcar op lst))


(provide 'accumulate)
;;; accumulate.el ends here
